test_alarm_sound:
  name: Test Alarm Sound
  description: Spielt den konfigurierten Alarmton zur Überprüfung ab
  fields:
    entity_id:
      name: Alarm Entity
      description: Die Alarm-Entität, deren Alarmton getestet werden soll
      required: true
      selector:
        entity:
          domain: alarm_control_panel
          integration: ha_alarm_pro

test_alarm:
  name: Test Alarm
  description: Löst einen Test-Alarm aus (triggert die Alarmanlage ohne echten Sensor-Trigger)
  fields:
    entity_id:
      name: Alarm Entity
      description: Die Alarm-Entität, die getestet werden soll
      required: true
      selector:
        entity:
          domain: alarm_control_panel
          integration: ha_alarm_pro

get_dashboard_yaml:
  name: Dashboard YAML abrufen
  description: Generiert die Dashboard-Konfiguration basierend auf deinen Einstellungen und zeigt sie als Benachrichtigung an
